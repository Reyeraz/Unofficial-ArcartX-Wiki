# 基础语法

Shimmer语法教学基础篇
# 基础语法
学习Shimmer的基本语法规则
在极端光明或黑暗中，看不清事物
## 1.注释：给代码加说明
📝
什么是注释?
注释是写给人看的说明文字，Shimmer会自动忽略这些内容。好的注释能帮助别人`包括将来的你自己`更容易理解代码。
### 两种注释方式
使用 `//` 添加单行注释:
    
    // 这是一个打招呼的程序
    Shimmer.println('你好!')
    // 下面这行输出再见
    Shimmer.println('再见!')
使用 `/*` 和 `*/` 包裹多行注释:
    
    /* 这是一个展示多行注释的例子
       可以写很多行
       都会被认为是注释
    */
    Shimmer.println('看到了吗?')
## 2.代码结构
在Shimmer中，代码结构非常简单直观，每一行就是一个完整的指令。
### 基本规则
    
    Shimmer.println('第一行')
    Shimmer.println('第二行')
    Shimmer.println('第三行')
每行代码都是独立的指令，按顺序从上到下执行。
当我们需要把多行代码组织在一起时，使用花括号 `{ }`:
    
    if(true){
        Shimmer.println('这是代码块内的第一行')
        Shimmer.println('这是代码块内的第二行')
    }
## 3.语句规范
⌨️
### 空格与缩进
  * 运算符前后建议加空格: `1 + 1`
  * 代码块内的内容要缩进(Shimmer无视缩进 但是建议养成习惯)，让结构更清晰


✒️
### 标点符号
  * 字符串要用引号: `'你好'`
  * 括号要成对出现: `()`、`{}`
  * 不需要在每行末尾加分号


### 良好的代码风格示例
    
    // 计算并显示结果
    result = 10
    if(result > 5){
        /* 如果数字大于5
           就显示下面的内容 */
        Shimmer.println("这个数字是: {result}")
        Shimmer.println('它大于5!')
    }
### 特别说明
    
    // Shimmer的“块”中 必须存在内容 不允许出现空块
    result = 10
    if(result > 5){
       // 如果确实没有什么好执行的 可以写个0占位
       0
    }
养成良好的代码书写习惯很重要！整洁的代码可以:
  * 减少出错
  * 方便阅读
  * 易于修改


## 实践练习
📝
### 练习1：添加注释
为下面的代码添加适当的注释:
    
    Shimmer.println('小猫')
    Shimmer.println('小狗')
    Shimmer.println('小兔子')
🎯
### 练习2：代码整理
把下面的代码按照规范格式重新整理:
    
    result = 5 + 3
    if( result > 7 ){
      Shimmer.println('大于7')
    }
    Shimmer.println('结果是:{result}')
  * 点击运行，看看是不是输出了你想要的内容！
  * PS:你可以直接进入在线调试工具的独立窗口，效果更佳 [点我进入](https://shimmer.arcartx.com/Shimmer.println\('%E4%BD%A0%E5%A5%BD%E4%B8%96%E7%95%8C'\))


记住：写代码就像写文章，整洁清晰的格式能让人更容易理解你想表达的内容。
[Previous你好，世界](/docs/shimmer/1_base/1_basic/1_first_shimmer)[Next数据类型](/docs/shimmer/1_base/1_basic/3_data_type)
