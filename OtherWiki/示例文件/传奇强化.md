```yml
# UI配置的根节点，定义整个强化界面的基础属性
ui:
  match: 强化界面  # 匹配的界面标识，用于识别该UI对应的界面
  hide: []  # 需要隐藏的HUD列表，这里为空表示不隐藏任何HUD
  itemSize: "16"  # 鼠标拿起物品时的渲染大小
  through: "true"  # 开启穿透点击，允许点击穿透当前UI触发下层元素交互
  escClose: "true"  # 允许按ESC键关闭该UI
  background: false  # 不渲染默认半透明黑色背景
  closeDied: "true"  # 玩家死亡时自动关闭该UI
  show: "true"  # 初始状态为显示该UI
  jei: "false"  # 不显示JEI侧边栏（如果安装了JEI模组）
  action:  # UI的事件触发器
    tick: |-  # 每帧执行的脚本
      var.强化物品 = val.成品.getSlotItemStack()  # 获取"成品"槽位中的物品，存储到变量供后续使用

# 控件配置区域，定义界面中的所有可视元素和交互组件
controls:
  # 自适配布局控件，作为根容器，用于适配不同屏幕分辨率
  adaptive:
    type: adaptive  # 控件类型：自适配布局
    attribute:  # 控件属性
      point: ~middle_center  # 锚点位置：屏幕正中心
      width: 1920  # 设计时的宽度（用于适配计算）
      height: 1080  # 设计时的高度（用于适配计算）
    children:  # 子控件列表
      # 背包背景贴图
      背包贴图:
        type: Texture  # 控件类型：纹理（图片）
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          width: 1276  # 纹理宽度
          height: 788  # 纹理高度
          normal: "~ui/ui4/ui.png"  # 常态下的纹理资源路径

      # 全部物品展示区域（使用网格布局）
      全部物品:
        type: Grid  # 控件类型：网格布局
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          x: -443  # X轴偏移量（相对于锚点）
          y: 37   # Y轴偏移量（相对于锚点）
          spaceBetweenY: 5  # 网格行间距
          spaceBetweenX: 5  # 网格列间距
          column: 4  # 网格列数
        action:
          create: |  # 创建时执行的脚本（初始化网格槽位）
            // 循环创建34个槽位（range(1,35)生成1到34的数字）
            for(a in range(1,35)){
              // 复制"全部物品槽位"作为模板，创建新槽位并命名
              slot = self['全部物品槽位'].copy('slot' + a)
              // 设置新槽位的ID（从55开始，54+1=55到54+34=88）
              slot.id = 54 + a
            }
        children:
          # 网格槽位的模板（会被上面的脚本复制）
          全部物品槽位:
            type: slot  # 控件类型：物品槽位
            attribute:
              normal: "~ui/ui4/normal2.png"  # 常态下的槽位纹理
              hover: "~ui/ui4/hover2.png"   # 鼠标悬停时的槽位纹理
              width: 75  # 槽位宽度
              height: 75  # 槽位高度
              itemScale: 0.7  # 槽位内物品的缩放比例
              id: 54  # 槽位ID（基础ID，复制时会递增）

      # 强化物品槽位（放置待强化的物品）
      强化物品槽位:
        type: slot  # 控件类型：物品槽位
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          normal: "~ui/ui4/normal2.png"  # 常态纹理
          hover: "~ui/ui4/hover2.png"   # 悬停纹理
          y: -120  # Y轴偏移（向上120单位）
          width: 240  # 大尺寸槽位宽度
          height: 240  # 大尺寸槽位高度
          itemScale: 0.7  # 物品缩放
          id: 13  # 槽位ID

      # "装备栏"文本标签
      装备栏:
        type: text  # 控件类型：文本
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          texts: ~装备栏  # 文本内容（~表示纯文本）
          fontSize: 60  # 字体大小
          x: 44  # X轴偏移
          y: -270  # Y轴偏移（向上270单位）
          center: true  # 文本居中对齐

      # 强化材料区域（网格布局）
      强化材料:
        type: Grid  # 控件类型：网格布局
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          x: 0  # X轴偏移
          y: 220  # Y轴偏移（向下220单位）
          spaceBetweenY: 5  # 行间距
          spaceBetweenX: 5  # 列间距
          column: 4  # 列数
        action:
          create: |  # 创建时执行的脚本
            // 循环创建2个材料槽位（range(1,3)生成1到2的数字）
            for(a in range(1,3)){
              // 复制模板槽位并命名
              slot = self['强化材料槽位'].copy('slot' + a)
              // 设置槽位ID（37和38）
              slot.id = 36 + a
            }
        children:
          # 强化材料槽位模板
          强化材料槽位:
            type: slot  # 控件类型：物品槽位
            attribute:
              normal: "~ui/ui4/normal2.png"  # 常态纹理
              hover: "~ui/ui4/hover2.png"   # 悬停纹理
              width: 100  # 槽位宽度
              height: 100  # 槽位高度
              itemScale: 0.7  # 物品缩放
              id: 36  # 基础ID

      # "所需材料"文本标签
      所需材料:
        type: text  # 控件类型：文本
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          texts: ~所需材料  # 文本内容
          fontSize: 60  # 字体大小
          x: 60  # X轴偏移
          y: 90  # Y轴偏移
          center: true  # 居中对齐

      # 保护符槽位（放置保护石）
      保护符槽位:
        type: slot  # 控件类型：物品槽位
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          normal: "~ui/ui4/normal2.png"  # 常态纹理
          hover: "~ui/ui4/hover2.png"   # 悬停纹理
          width: 100  # 槽位宽度
          height: 100  # 槽位高度
          x: 406  # X轴偏移（向右406单位）
          y: 170  # Y轴偏移
          itemScale: 0.7  # 物品缩放
          id: 11  # 槽位ID

      # "保护石"文本标签
      保护:
        type: text  # 控件类型：文本
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          texts: ~保护石  # 文本内容
          fontSize: 60  # 字体大小
          x: 452  # X轴偏移
          y: 90  # Y轴偏移
          center: true  # 居中对齐

      # 成品展示区域（强化后物品预览）
      成品展示:
        type: Texture  # 控件类型：纹理（作为背景框）
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          width: 280  # 宽度
          height: 320  # 高度
          x: 406  # X轴偏移
          y: -120  # Y轴偏移
          normal: ~0,0,0,255  # 黑色半透明背景（RGBA值）
        children:
          # "下级预览"文本标签
          下级预览:
            type: text  # 控件类型：文本
            attribute:
              point: ~middle_center  # 锚点位置：父容器正中心
              texts: ~下级预览  # 文本内容
              fontSize: 60  # 字体大小
              x: 140  # X轴偏移（相对于父容器）
              y: 20  # Y轴偏移
              center: true  # 居中对齐

          # 下级预览文字（显示强化后物品的描述）
          下级预览文字:
            type: text  # 控件类型：文本
            attribute:
              point: ~middle_center  # 锚点位置：父容器正中心
              texts: var.强化物品.getLore()  # 文本内容：从变量获取强化物品的描述（lore）
              fontSize: 40  # 字体大小
              x: 10  # X轴偏移
              y: 60  # Y轴偏移
              left: true  # 左对齐

      # 强化按钮
      强化按钮:
        type: Texture  # 控件类型：纹理（作为按钮背景）
        attribute:
          point: ~middle_center  # 锚点位置：父容器正中心
          width: 141  # 按钮宽度
          height: 48  # 按钮高度
          y: 300  # Y轴偏移（向下300单位）
          x: 179  # X轴偏移
          normal: "~ui/ui4/normal1.png"  # 常态纹理
          hover: "~ui/ui4/hover1.png"   # 悬停纹理
        children:
          # "强化"文本标签（按钮文字）
          强化:
            type: text  # 控件类型：文本
            attribute:
              point: ~middle_center  # 锚点位置：父容器正中心
              texts: ~强化  # 文本内容
              fontSize: 50  # 字体大小
              x: 70  # X轴偏移（相对于按钮中心）
              y: 10  # Y轴偏移
              center: true  # 居中对齐

          # 强化槽位（用于触发强化逻辑的隐藏槽位）
          强化槽位:
            type: slot  # 控件类型：物品槽位
            attribute:
              point: ~middle_center  # 锚点位置：父容器正中心
              width: 141  # 与按钮同宽
              height: 48  # 与按钮同高
              itemScale: 0  # 物品缩放为0（隐藏物品显示）
              id: 31  # 槽位ID

          # 成品槽位（用于存储强化后的物品）
          成品:
            type: slot  # 控件类型：物品槽位
            val: 成品  # 槽位标识（供脚本引用）
            attribute:
              point: ~middle_center  # 锚点位置：父容器正中心
              width: 0  # 宽度为0（隐藏槽位）
              height: 0  # 高度为0（隐藏槽位）
              itemScale: 0  # 物品不显示
              id: 15  # 槽位ID
```